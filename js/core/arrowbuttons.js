/**
 * ArrowButtons Compatibility Layer
 * This file now simply imports and re-exports the Trackpad class
 * for backward compatibility with existing code.
 */

import Trackpad from './trackpad.js';

// Export Trackpad as ArrowButtons for backward compatibility
export default Trackpad;
